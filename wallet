<!DOCTYPE html>
<html lang="nl">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Flarde | Toegang via TRIUWA</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      background: #111;
      color: #eee;
      padding: 2rem;
      text-align: center;
    }
    h1 {
      color: #fff;
    }
    .wallet-box {
      background: #222;
      padding: 2rem;
      margin-top: 2rem;
      border: 1px solid #444;
      border-radius: 10px;
      max-width: 500px;
      margin-left: auto;
      margin-right: auto;
    }
    button {
      background: #fff;
      color: #111;
      padding: 0.8rem 2rem;
      font-weight: bold;
      border: none;
      border-radius: 5px;
      cursor: pointer;
      margin-top: 1rem;
    }
    .access {
      margin-top: 2rem;
    }
  </style>
</head>
<body>
  <h1>🔗 Flarde | Wallet toegang</h1>

  <div class="wallet-box">
    <p><strong>Koppel je wallet om toegang te krijgen.</strong></p>
    <p>TRIUWA WUW Token vereist op Ethereum-netwerk.<br/>Token: <code>0x1c907176ee3920d41e9afc497148de62fca48893</code></p>
    <button onclick="connectWallet()">Connect Wallet</button>
    <div id="wallet-status"></div>
  </div>

  <div class="access" id="access-area" style="display: none;">
    <h2>✅ Toegang verleend</h2>
    <p>Welkom. Jouw TRIUWA-token geeft je toegang tot het archief.<br>Begin waar je wilt. Klim waar je kunt.</p>
    <a href="/bibliotheek.html" style="color: #0f0; font-weight: bold;">→ Ga naar de Bibliotheek</a>
  </div>

  <script>
    async function connectWallet() {
      if (typeof window.ethereum === 'undefined') {
        document.getElementById('wallet-status').innerText = '⚠️ MetaMask is niet geïnstalleerd.';
        return;
      }

      try {
        const [account] = await ethereum.request({ method: 'eth_requestAccounts' });
        const provider = new ethers.providers.Web3Provider(window.ethereum);
        const signer = provider.getSigner();
        const tokenAddress = '0x1c907176ee3920d41e9afc497148de62fca48893';
        const abi = ["function balanceOf(address) view returns (uint256)"];
        const contract = new ethers.Contract(tokenAddress, abi, signer);

        const balance = await contract.balanceOf(account);
        const readableBalance = Number(balance) / 1e9; // 9 decimalen

        if (readableBalance >= 10) {
          document.getElementById('access-area').style.display = 'block';
          document.getElementById('wallet-status').innerText = `✅ Wallet verbonden. Tokens: ${readableBalance}`;
        } else {
          document.getElementById('wallet-status').innerText = `🔒 Je hebt ${readableBalance} TRIUWA. Minimaal 10 nodig.`;
        }
      } catch (error) {
        console.error(error);
        document.getElementById('wallet-status').innerText = '⚠️ Verbinden mislukt.';
      }
    }
  </script>

  <!-- Ethers.js CDN -->
  <script src="https://cdn.jsdelivr.net/npm/ethers@5.7.2/dist/ethers.umd.min.js"></script>
</body>
</html>
